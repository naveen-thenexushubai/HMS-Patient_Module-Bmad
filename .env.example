# PostgreSQL
POSTGRES_DB=hospital_db
POSTGRES_USER=hospital_user
POSTGRES_PASSWORD=change_me_in_production

# Spring Boot
SPRING_PROFILES_ACTIVE=prod
JWT_SECRET=change_me_to_a_256bit_secret_in_production

# AES-256 Encryption (must be exactly 32 characters)
AES_ENCRYPTION_KEY=change_me_32chars_in_production!

# Twilio SMS — leave blank to use MockSmsProvider (dev/test mode)
# Get credentials from https://console.twilio.com
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=